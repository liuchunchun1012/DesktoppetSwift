# Meowpal 🐱 v2.0

一只可爱的 AI 桌面宠物！住在你的屏幕上，随时陪你聊天、帮你工作。

![演示](demo.gif)

## ✨ 功能特性

### 核心功能
- 🎨 **宠物动画** - 流畅的像素风动画（行走、休息、互动等）
- 🤖 **多 AI 服务商** - 支持 Ollama / OpenAI / Claude / Gemini / Grok / 通义千问 / DeepSeek / 自定义 API

### 工具 & 快捷键
- ⌨️ **全局快捷键**（可自定义）
  - `Cmd+Shift+J` - 打开聊天对话框
  - `Cmd+Shift+T` - 翻译剪贴板文本
  - `Cmd+Shift+L` - 分析剪贴板截图（支持追问）
  - `Cmd+Shift+K` - 划词助手工具栏
- ✂️ **划词助手** - 一键翻译、解释、总结或搜索选中文本
- 🔄 **40+ 种翻译语言** - 支持全球主要语言（English、简体中文、繁體中文、Español、Français、Deutsch、日本語、한국어 等）

### 同步 & 集成
- 📓 **Obsidian 集成** - 保存聊天、每日总结和任务到 Obsidian 库
- 📝 **Notion 集成** - 每日总结和 TodoList 任务创建
- 🔄 **灵活同步** - 可选择同步到 Notion、Obsidian 或两者都同步

## 📋 系统要求

- macOS 12.0 或更高版本
- **Intel Mac** 或 **Apple Silicon (M1/M2/M3/M4)**
- AI 服务（选择一个）：
  - **云端 API**：OpenAI / Claude / Gemini / Grok / 通义千问 / DeepSeek API Key（或 API2D 代理）
  - **本地模型**：[Ollama](https://ollama.ai) + 任意模型

## 🚀 快速开始

### 方式一：下载发布版（推荐）

> 💡 无需开发工具！下载、解压、运行即可。

1. 从 [Releases](https://github.com/liuchunchun1012/Meowpal/releases) 下载 **`Meowpal-Universal.zip`**
2. 解压后将 `Meowpal.app` 拖入应用程序文件夹（可选）
3. 双击运行。如被 macOS 阻止，前往 **系统设置 > 隐私与安全性** 点击 **仍要打开**
4. 点击菜单栏的 🐱 图标 → **Settings**，选择 AI 服务商并输入 API Key

**支持**：Intel Mac (x86_64) 和 Apple Silicon (M1/M2/M3/M4)

---

### 方式二：一键安装脚本（本地模型用户）

> 💡 自动安装 Ollama 和模型，构建并启动应用。

```bash
git clone https://github.com/liuchunchun1012/Meowpal.git
cd Meowpal
./install.sh
```

脚本将：
- 检测并安装 Command Line Tools
- 可选安装 Homebrew 和 Ollama
- 可选下载推荐模型（gemma3、qwen3、llava 等）
- 构建并启动应用

---

### 方式三：从源码构建（开发者）

```bash
# 安装 Command Line Tools（如需要）
xcode-select --install

# 克隆项目
git clone https://github.com/liuchunchun1012/Meowpal.git
cd Meowpal

# 构建通用版本（Intel + Apple Silicon）
./package_universal.sh

# 运行
open Meowpal.app
```

---

## ⚙️ 配置指南

### 可视化设置（推荐）

点击菜单栏的 🐱 图标 → **Settings** 进行配置：

| 标签页 | 选项 |
|--------|------|
| **AI Settings** | 服务商选择、API Key、Base URL、模型选择、测试连接 |
| **Advanced** | Temperature、Top-P、Max Tokens、联网搜索开关 |
| **Appearance** | 自定义精灵图路径 |
| **Tools** | 划词助手开关、翻译目标语言 |
| **Hotkeys** | 自定义聊天/翻译/图片/划词快捷键 |
| **Notion** | API Token、Database ID、测试连接 |
| **Obsidian** | Vault 路径、快速保存文件夹 |

### 支持的 AI 服务商

| 服务商 | 说明 | 需要 API Key |
|--------|------|-------------|
| **Ollama** | 本地运行，完全免费 | ❌ |
| **OpenAI** | 同步最新模型 | ✅ |
| **Anthropic** | 同步最新模型 | ✅ |
| **Google Gemini** | 同步最新模型 | ✅ |
| **xAI Grok** | 同步最新模型 | ✅ |
| **通义千问** | 同步最新模型 | ✅ |
| **DeepSeek** | 文本对话，支持通过 SearXNG 工具调用联网搜索 | ✅ |
| **自定义 API** | OpenAI 兼容服务（如 API2D） | ✅ |

DeepSeek 联网搜索使用模型工具调用加 SearXNG。为了稳定可用，推荐本地 Docker 运行 SearXNG，并在 Advanced Settings 中填写地址。

### DeepSeek 联网搜索设置

公开 SearXNG 实例经常禁用 JSON 输出或限制 API 请求。推荐本地运行 SearXNG：

```bash
docker run -d --name searxng -p 8080:8080 searxng/searxng
```

在容器里启用 JSON 输出：

```bash
docker exec searxng sh -lc "cp /etc/searxng/settings.yml /etc/searxng/settings.yml.bak && sed -i '/^[[:space:]]*- html$/a\    - json' /etc/searxng/settings.yml"
docker restart searxng
```

验证 JSON 是否可用：

```bash
curl -i 'http://localhost:8080/search?q=test&format=json'
```

预期看到：

```text
HTTP/1.1 200 OK
content-type: application/json
```

然后打开 **Settings → AI Settings → Advanced Settings**：

- 打开 `Enable Web Search`
- 将 `SearXNG URL` 设置为 `http://localhost:8080`
- 点击保存

注意：`SearXNG URL` 不要包含 `/search`。

---

## 📖 使用方法

### 基础交互

| 操作 | 说明 |
|------|------|
| 拖拽 | 移动宠物位置 |
| 点击 | 触发跳跃动画 |
| 悬停 | 触发随机互动（揉肚子/拒绝） |
| 菜单栏图标 | 设置、切换动作、退出 |

### 快捷键功能

> 💡 所有快捷键可在 Settings → Hotkeys 中自定义

#### 💬 聊天 `Cmd+Shift+J`
打开输入框，输入后按回车，小猫用 AI 回复你。支持追问！

#### 🌐 翻译 `Cmd+Shift+T`
1. 选中文本 → `Cmd+C` 复制
2. 按 `Cmd+Shift+T`
3. 翻译结果显示在聊天气泡中

#### 📸 截图分析 `Cmd+Shift+L`
1. 截图（推荐 [Shottr](https://shottr.cc/) 或微信截图）
2. 按 `Cmd+Shift+L`，输入框出现
3. 输入问题（如"这是什么？""帮我看看这道题"）
4. 小猫分析图片并回答。**支持追问！**

#### ✂️ 划词助手 `Cmd+Shift+K`
1. 选中任意文字 → `Cmd+C` 复制
2. 按 `Cmd+Shift+K`，工具栏出现在光标附近
3. 点击：翻译 / 解释 / 总结 / 搜索

#### ➕ 快速添加任务
消息以 `+` 开头即可创建任务：
```
+明天下午3点买菜
+周五10点-11点团队会议
+本周末前完成报告
```
AI 会自动解析时间、优先级，并添加到你的任务列表。

**保存位置：** Notion、Obsidian 或两者都保存（在 Settings → Tools 中选择）
- **Notion**：创建结构化数据库条目
- **Obsidian**：追加到 Tasks.md，使用 [Obsidian Tasks](https://obsidian-tasks-group.github.io/obsidian-tasks/) 格式

### 💾 一键保存到 Obsidian

聊天气泡右上角有保存按钮，点击可保存**完整对话**（问题 + 回答 + 图片）到 Obsidian 库。

---

## 🐛 常见问题

### 快捷键无效？
1. 确认应用正在运行（菜单栏有猫咪图标）
2. 首次运行需在 系统设置 > 隐私与安全性 > 辅助功能 中授权

### 小猫不回复？

**使用 Ollama：**
```bash
# 测试 Ollama 是否运行
curl http://localhost:11434/api/tags

# 如无响应，启动 Ollama
ollama serve
```

**使用云端 API：**
- 检查 API Key 是否正确
- 点击 Settings → Test Connection 验证

### 截图分析无效？
需要支持视觉的模型：
- Ollama：`gemma3`、`llava` 等
- 云端：GPT-4o、Claude 3.5 Sonnet、Gemini、Grok 2 Vision 等

---

## 🛠️ 技术栈

- **SwiftUI** - UI 框架
- **AppKit** - 窗口管理
- **Keychain Services** - API Key 安全存储
- **Carbon Framework** - 全局快捷键

## 📜 许可证

本项目采用 MIT 许可证 - 详见 [LICENSE](LICENSE) 文件。

## 🎉 致谢

- 感谢 [Ollama](https://ollama.ai) 提供本地 LLM 方案
- 宠物精灵图灵感来源：[Stardew Valley]

### ☕ 支持者

| 支持者 | 金额 | 日期 | 备注 |
|--------|------|------|------|
| **peachesmeow** | ¥66.66 | 2025-12-21 | 🎉 Meowpal 的第一位用户和支持者！ |

## 💖 支持项目

如果这个项目对你有帮助：
- 给个 Star ⭐️
- 分享给朋友
- 提交 Issue 或 PR

如果你想请我喝杯咖啡：

[![ko-fi](https://ko-fi.com/img/githubbutton_sm.svg)](https://ko-fi.com/liuchun)

<table>
  <tr>
    <td align="center">
      <img src="assets/alipay.jpg" width="200px" /><br />
      <b>支付宝</b>
    </td>
    <td align="center">
      <img src="assets/wechat.jpg" width="200px" /><br />
      <b>微信</b>
    </td>
  </tr>
</table>

---

**Happy Coding!** 🐱
